# Telegram-To-Do-Bot-with-SQLite-FSM
A Python-based Telegram bot for managing tasks. Features include a persistent SQLite database, Finite State Machine (FSM) for user states, and full CRUD operations (Create, Read, Delete).
